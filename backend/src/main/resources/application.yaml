spring:
  application:
    name: auth-study
  profiles:
    default: dev
  jpa:
    hibernate:
      ddl-auto: validate

server:
  servlet:
    session:
      cookie:
        name: ${app.oauth.session-cookie-name}
